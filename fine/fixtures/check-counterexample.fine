// Subtraction does not preserve its left operand as a lower bound once the
// subtracted value is positive. Fine should return the actual failing inputs.
check subtraction_preserves_left(a: Int, b: Int) {
  assumes {
    a <= -1;
    b >= 1;
  }
  ensures {
    a - b >= a;
  }
}
