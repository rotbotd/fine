enum Name {
  a,
  b,
}

predicate Cofinite(body: Name) {
  constructor under(name: Name, body: Name) {
    takes {
      Cofinite(name);
    }
    gives Cofinite(body);
  }
}

check impossible() {
  assumes {}
  ensures {
    Cofinite(a);
  }
}
