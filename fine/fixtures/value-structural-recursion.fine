enum Nat {
  zero,
  succ(Nat),
}

function copy(value: Nat) -> Nat {
  match value {
    zero => zero,
    succ(previous) => succ(copy(previous)),
  }
}

run value_structural_recursion {
  let two: Nat = succ(succ(zero));
  let copied: Nat = copy(two);
  assert copied == two;
}
