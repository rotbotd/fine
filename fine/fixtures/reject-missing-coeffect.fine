function replace(left: Int, right: Int) -> Int
  takes [same: Id(Int, left, right)]
  ensures {
    result == right;
  }
{
  left
}

run missing_coeffect {
  let x: Int = 7;
  let answer: Int = replace(x, x);
  assert answer == x;
}
